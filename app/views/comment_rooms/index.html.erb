<div class="container">
	<div class="row">
		<div class="col-xs-8">
			<h3>コメントルームを選択してください</h3>
		</div>
		<div class="col-xs-4">
			<%= link_to "コメントルーム新規作成", "/sports/comment_rooms/new/#{@sport.id}", class: "btn btn-primary" %>		
		</div>
		<div class="col-xs-6">
			<table class="table">
		  <% @comment_rooms.each do |room| %>
			<tr>
			<td><%= link_to room.title, comment_room_path(room)  %></td>
	  	</tr>
			<% end %>
		</table>
		</div>
		</div>
	</div>
</div>