<!DOCTYPE html>
<html>
<head>
  <title>SportsCompanion</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body data-turbolinks="false">
  <% if user_signed_in? %>
  <!-- ユーザーのログイン状況によりヘッダーを変更 -->
  <%= render "layouts/header_login" %>
  <% else %>
  <%= render "layouts/header_logout" %>
  <% end %>   
  <% if notice %>
  <h5 class="notice"><%= notice %></h5>
  <% end %>
  <%= yield %>

  <footer>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-sample">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbar-sample">
        <ul class="navbar-nav">
          <li><%= link_to "トップに戻る", root_path, class: "navbar-brand navbar-left" %></li>
          <li><%= link_to "お問い合わせ", inquiry_new_path, class: "nav-link" %></li>
        </ul>
      </div>
    </nav>
  </footer>
</body>
</html>
